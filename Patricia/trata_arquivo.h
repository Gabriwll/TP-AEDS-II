#ifndef TRATA_ARQUIVO_H
#define TRATA_ARQUIVO_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

char* trata_N(char *palavra);
char* minusculo(char *palavra);
char* trata_P(char *palavra);

#endif